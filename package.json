{
  "name": "gym-tracker",
  "version": "1.0.0",
  "description": "Gym Tracker web app, packaged for Android via Capacitor",
  "private": true,
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "cap:sync": "npm run build:www && npx cap sync android",
    "cap:open": "npx cap open android",
    "android:devices": "npx cap run android --list",
    "android:install": "npm run cap:sync && npx cap run android",
    "android:apk": "npm run cap:sync && node scripts/assemble-apk.js"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.0"
  }
}
